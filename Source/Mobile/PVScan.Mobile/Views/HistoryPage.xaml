<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
            
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:PVScan.Mobile.ViewModels"
    x:Class="PVScan.Mobile.Views.HistoryPage"
    
    BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundColorDarker}, Light={StaticResource BackgroundColorLighter}}">

    <ContentView.BindingContext>
        <viewmodels:HistoryPageViewModel/>
    </ContentView.BindingContext>
    
    <Grid>
        <CollectionView ItemsSource="{Binding Barcodes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid BackgroundColor="{StaticResource BackgroundColorDarkerer}" HeightRequest="65">
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Id}" TextColor="{StaticResource TextPrimaryColor_Dark}"/>
                            <Label Text="{Binding Text}" TextColor="{StaticResource TextPrimaryColor_Dark}"/>
                        </StackLayout>
                        <BoxView HeightRequest="1" BackgroundColor="{StaticResource BackgroundColorDark}" VerticalOptions="End"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <StackLayout>
                    <Label VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"
                           Text="No blogs yet"/>
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>
    </Grid>
</ContentView>